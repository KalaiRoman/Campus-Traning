function names() {
    var a = parseInt(prompt("enter the first number"));
    var b = parseInt(prompt("enter the  second numbver:"));
    var sum = a + b;
    document.getElementsByClassName("add").innerHTML = alert("This number is add:     " + sum);

}